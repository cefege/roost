//! Agent status ordering: the total order a list and a broadcast both read,
//! so a reordering is one answer rather than two. Owned by the agent slice;
//! the module declaration lives in `agents/mod.rs`.
