//! Cloudflare Access identity: turning a verified edge assertion into the
//! same principal an authorized key produces, so a fronted coordinator and a
//! direct one answer the same way. Owned by the devices slice; the module
//! declaration lives in `auth/mod.rs`.
