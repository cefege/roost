//! Bootstrap tokens: minting one, redeeming it from a worker or a browser,
//! and the single-use lifetime that makes one worth minting. Owned by the
//! devices slice; the module declaration lives in `auth/mod.rs`.
