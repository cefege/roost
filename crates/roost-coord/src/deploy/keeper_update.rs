//! Preparing a keeper update: the exclusive write drain, the admission
//! decision, and what the worker is told to converge on. Owned by the keeper
//! update slice; the module declaration lives in `deploy/mod.rs`.
