//! The caller's origin profile: what a request's Host and Origin headers
//! prove about who is calling, and which of them a given route demands.
//! Owned by the middleware slice; the module declaration lives in
//! `middleware/mod.rs`.
