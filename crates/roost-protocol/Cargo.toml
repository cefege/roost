[package]
name = "roost-protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Pure I/O-free wire and terminal logic: event variants and the canonical fold, cell and grid models, viewport geometry, peer packet framing, the keeper-update contract, and layout documents. Builds for wasm32 so browser, native, coordinator and worker share one implementation."

[lints]
workspace = true
