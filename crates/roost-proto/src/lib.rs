//! Generated protobuf messages and Connect service stubs, built from protocol/proto/roost/v1 by connectrpc-build. Owned by nobody — every crate that speaks the wire depends on it. Reads the .proto tree at build time and nothing else does.
//! Owned here until a phase fills it in; nothing calls it yet.

#![forbid(unsafe_code)]
